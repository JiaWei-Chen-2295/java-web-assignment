<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fun.javierchen.noteapp.mapper.UserMapper">

    <resultMap id="UserResultMap" type="fun.javierchen.noteapp.model.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="avatar" column="avatar"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT id, username, password, email, avatar, created_at, updated_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="selectByEmail" parameterType="string" resultMap="UserResultMap">
        SELECT id, username, password, email, avatar, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="selectById" parameterType="long" resultMap="UserResultMap">
        SELECT id, username, password, email, avatar, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="fun.javierchen.noteapp.model.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email, avatar, created_at, updated_at)
        VALUES (#{username}, #{password}, #{email}, #{avatar}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="fun.javierchen.noteapp.model.entity.User">
        UPDATE users
        SET username = #{username},
            password = #{password},
            email = #{email},
            avatar = #{avatar},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
