# Java Web 实验报告（ch01-ch06）

## 实验目的
通过本次实验掌握 JSP、Servlet、请求参数处理、请求头读取、登录校验、用户注册、表单 action 路径写法以及文件上传等 Java Web 核心知识，理解从页面到后端处理再到结果展示的完整实现流程。

## 实验环境
- 操作系统：Windows 11
- 开发工具：IntelliJ IDEA
- JDK 版本：JDK 17
- Servlet 规范：Jakarta Servlet 6.0 / 6.1
- 项目构建：Maven
- 项目目录：`D:\a_my_project\java-web`

## 实验项目说明
本次实验代码分布在 `ch01` 到 `ch06` 六个模块中，整体覆盖了题目要求的大部分实验内容。个别题目与当前仓库实现存在轻微差异，已在对应小节中如实说明。

---

## 1. 建立 JSP 文件并运行

### 实验要求
1. 在 `F:` 盘下建立站点文件夹 `2026javaweb`
2. 设置 Eclipse 与 Tomcat 的连接
3. 新建动态项目 `ch01`
4. 新建 `first.jsp`，显示学号姓名、当前日期和时间、`Hello, 2026!`、`我是*专业+学号+姓名`
5. 运行 JSP 文件

### 实验过程
1. 题目要求先在 `F:` 盘建立站点文件夹 `2026javaweb`，该步骤属于实验环境搭建过程，当前代码仓库无法直接体现，只能在报告中按要求说明。
2. 题目要求配置 Eclipse 与 Tomcat 的连接，这同样属于环境配置步骤，仓库中没有保存该界面信息，因此不能写成仓库已直接证明的事实。
3. 从当前仓库真实结构看，实验 1 对应的项目目录是 `D:\a_my_project\java-web\ch01`。
4. 真实 JSP 文件路径是 `D:\a_my_project\java-web\ch01\src\main\webapp\first.jsp`。
5. 页面使用 JSP 表达式输出当前日期和时间，并写入学号、姓名以及专业信息。
6. `D:\a_my_project\java-web\ch01\src\main\webapp\WEB-INF\web.xml` 中将 `first.jsp` 配置为了欢迎页。

### 对应代码
文件：`D:\a_my_project\java-web\ch01\src\main\webapp\first.jsp`

```jsp
<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ page import="java.time.LocalDateTime" %>
<%@ page import="java.time.format.DateTimeFormatter" %>
<!DOCTYPE html>
<html>
<head>
    <title>ch01 - first.jsp</title>
</head>
<body>
    <div class="container">
        <h1>Hello, 2026!</h1>

        <p>学号姓名：<span class="info">2023154202 陈佳玮</span></p>

        <p>当前日期和时间：<span class="info">
            <%= LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy年MM月dd日 HH:mm:ss")) %>
        </span></p>

        <p>我是数据科学与大数据技术2023154202陈佳玮</p>
    </div>
</body>
</html>
```

### 结果说明
从真实文件 `D:\a_my_project\java-web\ch01\src\main\webapp\first.jsp` 可以确认，页面已经实现了 `Hello, 2026!`、学号姓名、当前日期时间以及“我是数据科学与大数据技术2023154202陈佳玮”四项显示内容。

### 截图说明
请在此处插入 `first.jsp` 运行截图：

```md
![first.jsp运行结果](./images/ch01-first-jsp.png)
```

---

## 2. 新建第一个 Servlet 文件

### 实验要求
新建第一个 Servlet 文件，显示欢迎信息、学号、姓名。

### 实验过程
1. 真实 Servlet 文件路径为 `D:\a_my_project\java-web\ch01\src\main\java\fun\javierchen\WelcomeServlet.java`。
2. 该类继承 `HttpServlet`，重写 `doGet()` 方法。
3. 在方法中设置响应编码，使用 `PrintWriter` 向浏览器输出 HTML 内容。
4. `D:\a_my_project\java-web\ch01\src\main\webapp\WEB-INF\web.xml` 将该 Servlet 映射到 `/welcome`。
5. 同一个 `web.xml` 还将 `first.jsp` 设置为欢迎页。

### 对应代码
文件：`D:\a_my_project\java-web\ch01\src\main\java\fun\javierchen\WelcomeServlet.java`

```java
public class WelcomeServlet extends HttpServlet {

    @Override
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

        response.setContentType("text/html;charset=UTF-8");
        PrintWriter out = response.getWriter();

        String currentTime = LocalDateTime.now().format(
            DateTimeFormatter.ofPattern("yyyy年MM月dd日 HH:mm:ss")
        );

        out.println("<h1>欢迎来到 Java Web 开发世界！</h1>");
        out.println("<p>学号：<span class=\"info\">2023154202</span></p>");
        out.println("<p>姓名：<span class=\"info\">陈佳玮</span></p>");
        out.println("<p>当前时间：<span class=\"info\">" + currentTime + "</span></p>");
    }
}
```

文件：`D:\a_my_project\java-web\ch01\src\main\webapp\WEB-INF\web.xml`

```xml
<servlet>
    <servlet-name>WelcomeServlet</servlet-name>
    <servlet-class>fun.javierchen.WelcomeServlet</servlet-class>
</servlet>
<servlet-mapping>
    <servlet-name>WelcomeServlet</servlet-name>
    <url-pattern>/welcome</url-pattern>
</servlet-mapping>
```

### 结果说明
访问 Servlet 后，浏览器页面能够输出欢迎信息、学号、姓名和当前时间，说明第一个 Servlet 创建成功。

### 截图说明
请在此处插入 `WelcomeServlet` 运行截图：

```md
![WelcomeServlet运行结果](./images/ch01-welcome-servlet.png)
```

---

## 3. 建立 GetTest.jsp 和 GetForm.jsp 程序

### 实验要求
参考课件建立 `GetTest.jsp` 程序，运行后在地址栏输入参数查看结果；继续建立 `GetForm.jsp`，运行、输入数据并查看结果，上传运行截图和代码。

### 实验过程
1. 真实表单页面路径为 `D:\a_my_project\java-web\ch02\src\main\webapp\GetForm.jsp`。
2. 页面中的表单写法是 `action="GetTest.jsp" method="get"`。
3. 真实结果页面路径为 `D:\a_my_project\java-web\ch02\src\main\webapp\GetTest.jsp`。
4. `GetTest.jsp` 通过 `request.getParameter("account")` 读取参数。
5. 提交后浏览器地址栏会出现类似 `GetTest.jsp?account=2023154202陈佳玮` 的参数形式。

### 对应代码
文件：`D:\a_my_project\java-web\ch02\src\main\webapp\GetForm.jsp`

```jsp
<form action="GetTest.jsp" method="get">
    <label>
        用户名： <input type="text" name="account">
    </label>
    <br>
    <input type="submit">
</form>
```

文件：`D:\a_my_project\java-web\ch02\src\main\webapp\GetTest.jsp`

```jsp
<body>
    <%= request.getParameter("account") %>
</body>
```

### 结果说明
提交表单后，参数通过 URL 地址栏传递，`GetTest.jsp` 成功读取并显示 `account` 参数值，说明 GET 请求参数获取成功。

### 截图说明
请在此处插入以下截图：

```md
![GetForm页面](./images/ch02-getform.png)
![GetTest运行结果](./images/ch02-gettest-result.png)
```

---

## 4. 设计 HeaderServlet 显示请求头信息

### 实验要求
设计 `HeaderServlet`，显示请求头信息。

### 实际实现说明
当前仓库 `ch03` 中未找到名为 `HeaderServlet` 的文件，真实存在且对应本题功能的文件是 `D:\a_my_project\java-web\ch03\src\main\java\fun\javierchen\ch03\param\ParamServlet.java`，它已经实现了“显示请求头信息”的核心功能。

### 实验过程
1. 在 `ch03` 项目中创建 Servlet。
2. 在请求处理方法中调用 `request.getHeaderNames()` 获取所有请求头名称。
3. 使用循环逐项输出请求头名称和对应值。
4. 同时输出请求方法、URI、URL、协议、客户端地址等辅助信息。

### 对应代码
文件：`D:\a_my_project\java-web\ch03\src\main\java\fun\javierchen\ch03\param\ParamServlet.java`

```java
@WebServlet(name = "paramServlet", value = "/param")
public class ParamServlet extends HttpServlet {

    private void processRequest(HttpServletRequest request, HttpServletResponse response) throws IOException {
        response.setContentType("text/plain;charset=UTF-8");
        PrintWriter out = response.getWriter();

        out.println("=== 请求信息 ===");
        out.println("Method: " + request.getMethod());
        out.println("URI: " + request.getRequestURI());
        out.println("URL: " + request.getRequestURL());
        out.println("QueryString: " + request.getQueryString());
        out.println("Protocol: " + request.getProtocol());
        out.println("RemoteAddr: " + request.getRemoteAddr());

        out.println();
        out.println("=== 请求头 ===");
        Enumeration<String> headerNames = request.getHeaderNames();
        while (headerNames.hasMoreElements()) {
            String name = headerNames.nextElement();
            out.println(name + ": " + request.getHeader(name));
        }
    }
}
```

### 结果说明
运行后可以查看浏览器提交到服务器的请求头信息，例如 `host`、`user-agent`、`accept-language` 等。

### 截图说明
请在此处插入请求头输出截图：

```md
![请求头信息输出](./images/ch03-header-servlet.png)
```

---

## 5. 建立 RequestServlet，显示客户端信息和个人信息

### 实验要求
建立 `RequestServlet`，显示客户端信息、个人学号姓名信息。

### 实际实现说明
当前仓库中最接近本题的真实文件位于 `D:\a_my_project\java-web\ch03\src\main\java\fun\javierchen\ch03\param`：
- `ParamServlet.java` 已显示客户端请求信息和请求头
- `RequestParamServlet.java` 已显示请求参数

仓库内未单独找到名为 `RequestServlet`、且同时直接输出“个人学号姓名”的类，因此本报告只按现有真实文件进行说明。

### 对应代码
文件：`D:\a_my_project\java-web\ch03\src\main\java\fun\javierchen\ch03\param\RequestParamServlet.java`

```java
@WebServlet(name = "requestParamServlet", value = "/request-param")
public class RequestParamServlet extends HttpServlet {

    private void processRequest(HttpServletRequest request, HttpServletResponse response) throws IOException {
        response.setContentType("text/plain;charset=UTF-8");
        PrintWriter out = response.getWriter();

        out.println("=== 请求参数 ===");
        Enumeration<String> paramNames = request.getParameterNames();
        if (!paramNames.hasMoreElements()) {
            out.println("(无参数)");
        } else {
            while (paramNames.hasMoreElements()) {
                String name = paramNames.nextElement();
                String value = request.getParameter(name);
                out.println(name + " = " + value);
            }
        }
    }
}
```

### 结果说明
通过地址栏或表单传参后，Servlet 可以显示客户端提交的参数内容。如果需要完全满足题意，可在后续完善时追加学号姓名固定信息输出。

### 截图说明
请在此处插入请求参数显示截图：

```md
![RequestServlet运行结果](./images/ch03-request-servlet.png)
```

---

## 6. 实现登录页面与登录结果页面

### 实验要求
建立 4 个文件：
- `login.jsp`
- `LoginServlet`
- `welcome.jsp`
- `failure.jsp`

要求用户登录成功显示用户名和当时时间，否则显示登录失败，并上传文件截图、运行截图。

### 实验过程
1. 真实登录页为 `D:\a_my_project\java-web\ch04\src\main\webapp\login.jsp`。
2. 真实后台处理类为 `D:\a_my_project\java-web\ch04\src\main\java\fun\javierchen\ch04\LoginServlet.java`。
3. 当用户名为 `2023154202陈佳玮` 且密码为 `123456` 时，Servlet 把用户名和当前时间保存到请求域。
4. 登录成功后转发到 `D:\a_my_project\java-web\ch04\src\main\webapp\welcome.jsp`。
5. 登录失败后转发到 `D:\a_my_project\java-web\ch04\src\main\webapp\failure.jsp`。

### 对应代码
文件：`D:\a_my_project\java-web\ch04\src\main\webapp\login.jsp`

```jsp
<form action="login" method="post">
    <p>
        用户名(学号+姓名)：<input type="text" name="username" placeholder="如：2023154202陈佳玮">
    </p>
    <p>
        密码：<input type="password" name="password">
    </p>
    <p>
        <input type="submit" value="登录">
    </p>
</form>
```

文件：`D:\a_my_project\java-web\ch04\src\main\java\fun\javierchen\ch04\LoginServlet.java`

```java
private static final String VALID_USERNAME = "2023154202陈佳玮";
private static final String VALID_PASSWORD = "123456";

protected void doPost(HttpServletRequest request, HttpServletResponse response) throws IOException, ServletException {
    request.setCharacterEncoding("UTF-8");

    String username = request.getParameter("username");
    String password = request.getParameter("password");

    if (VALID_USERNAME.equals(username) && VALID_PASSWORD.equals(password)) {
        String currentTime = LocalDateTime.now().format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
        request.setAttribute("username", username);
        request.setAttribute("loginTime", currentTime);
        request.getRequestDispatcher("welcome.jsp").forward(request, response);
    } else {
        request.getRequestDispatcher("failure.jsp").forward(request, response);
    }
}
```

文件：`D:\a_my_project\java-web\ch04\src\main\webapp\welcome.jsp`

```jsp
<h2>登录成功！</h2>
<p>欢迎您，<%= request.getAttribute("username") %></p>
<p>登录时间：<%= request.getAttribute("loginTime") %></p>
```

文件：`D:\a_my_project\java-web\ch04\src\main\webapp\failure.jsp`

```jsp
<h2>登录失败！</h2>
<p>用户名或密码错误</p>
```

### 结果说明
输入正确账号密码后，页面跳转到 `welcome.jsp`，显示用户名与当前时间；输入错误信息后跳转到 `failure.jsp`，显示登录失败。

### 截图说明
请在此处插入以下截图：

```md
![登录页面](./images/ch04-login-page.png)
![登录成功页面](./images/ch04-login-success.png)
![登录失败页面](./images/ch04-login-failure.png)
```

---

## 7. 设计用户注册页面和注册后台处理

### 实验要求
设计用户注册页面 `register.html`，注册后台处理 `registerServlet` 完成注册实现，演示账号为学号姓名，上传运行截图和文件代码。

### 实验过程
1. 真实注册页为 `D:\a_my_project\java-web\ch05\src\main\webapp\register.html`。
2. 页面中输入账号和密码，前端提示账号格式为“学号+姓名”。
3. 真实后台处理类为 `D:\a_my_project\java-web\ch05\src\main\java\fun\javierchen\ch05\RegisterServlet.java`。
4. Servlet 判断账号密码是否为空。
5. 服务器端读取 `WEB-INF/data/users.txt` 检查账号是否已存在。
6. 若不存在则将账号密码写入文件，注册成功后跳转 `success.html`，否则跳转 `failure.html`。

### 对应代码
文件：`D:\a_my_project\java-web\ch05\src\main\webapp\register.html`

```html
<form action="register" method="post">
    <div class="form-group">
        <label for="account">账号</label>
        <input type="text" id="account" name="account" required placeholder="请输入学号+名字">
        <div class="hint">格式：学号+名字，如 2024001张三</div>
    </div>
    <div class="form-group">
        <label for="password">密码</label>
        <input type="password" id="password" name="password" required placeholder="请输入密码">
    </div>
    <button type="submit">注册</button>
</form>
```

文件：`D:\a_my_project\java-web\ch05\src\main\java\fun\javierchen\ch05\RegisterServlet.java`

```java
String account = request.getParameter("account");
String password = request.getParameter("password");

if (account == null || account.trim().isEmpty() ||
    password == null || password.trim().isEmpty()) {
    response.sendRedirect("failure.html");
    return;
}

Path filePath = getDataFilePath();

if (isAccountExists(filePath, account)) {
    response.sendRedirect("failure.html");
    return;
}

saveUser(filePath, account, password);
response.sendRedirect("success.html");
```

### 结果说明
注册成功后用户数据被保存到服务器端 `users.txt` 文件中。仓库中已经存在运行产物：

- `D:\a_my_project\java-web\ch05\target\ch05-1.0-SNAPSHOT\WEB-INF\data\users.txt`

说明注册功能已实际执行成功。

### 说明
前端页面明确提示账号格式为“学号+姓名”，但当前后端代码只校验非空，并未强制校验格式，因此报告中应按实际实现如实描述。

### 截图说明
请在此处插入以下截图：

```md
![注册页面](./images/ch05-register-page.png)
![注册成功页面](./images/ch05-register-success.png)
![users.txt保存结果](./images/ch05-users-file.png)
```

---

## 8. 四种方式获取 action 文件路径

### 实验要求
在项目 `ch2` 下 `webapp` 建立文件夹 `form`，设计 4 个登录文件 `login.jsp` 和 1 个 `LoginServlet`，分别使用 4 种方式获取 action 文件路径：
1. `contextpath` 绝对路径
2. 相对路径
3. `server-relative` 相对路径
4. `page-relative` 相对路径

### 实际实现说明
题目要求写在 `ch2/webapp/form` 下，但当前仓库中的真实实现位于 `D:\a_my_project\java-web\ch05\src\main\webapp` 根目录，实际文件名分别为：
- `login1_contextpath.jsp`
- `login2_relative.jsp`
- `login3_server_relative.jsp`
- `login4_page_relative.jsp`

四种路径写法已经完整实现，只是模块名和目录位置与题目描述不完全一致。

### 四种写法

#### 方式 1：contextpath 绝对路径
文件：`D:\a_my_project\java-web\ch05\src\main\webapp\login1_contextpath.jsp`

```jsp
<form action="${pageContext.request.contextPath}/login" method="post">
```

#### 方式 2：相对路径
文件：`D:\a_my_project\java-web\ch05\src\main\webapp\login2_relative.jsp`

```jsp
<form action="login" method="post">
```

#### 方式 3：server-relative 路径
文件：`D:\a_my_project\java-web\ch05\src\main\webapp\login3_server_relative.jsp`

```jsp
<form action="/ch05/login" method="post">
```

#### 方式 4：page-relative 路径
文件：`D:\a_my_project\java-web\ch05\src\main\webapp\login4_page_relative.jsp`

```jsp
<form action="./login" method="post">
```

### 后台处理代码
文件：`D:\a_my_project\java-web\ch05\src\main\java\fun\javierchen\ch05\LoginServlet.java`

```java
String account = request.getParameter("account");
String password = request.getParameter("password");

if (validateUser(filePath, account, password)) {
    String currentTime = LocalDateTime.now()
            .format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
    response.getWriter().write(currentTime);
} else {
    response.getWriter().write("登录失败");
}
```

### 结果说明
四个页面均可向同一个 `/login` 提交数据，区别在于 `action` 地址写法不同。通过本实验可以理解不同路径在 Web 项目中的定位方式。

### 截图说明
请在此处插入以下截图：

```md
![contextpath路径登录](./images/ch05-login1-contextpath.png)
![relative路径登录](./images/ch05-login2-relative.png)
![server-relative路径登录](./images/ch05-login3-server-relative.png)
![page-relative路径登录](./images/ch05-login4-page-relative.png)
```

---

## 9. 单文件上传与多文件上传

### 实验要求
完成单独文件上传和多个文件上传，显示学号、姓名、时间、提交成功信息，提交文件代码、文件上传成功截图、文件保存截图。

### 实际实现说明
当前 `ch06` 模块使用一个统一页面 `upload.html`，通过 `<input type="file" name="files" multiple>` 同时支持单文件和多文件上传，没有拆成两个独立页面，但功能上已经覆盖单文件与多文件两种情况。

### 实验过程
1. 真实上传页面为 `D:\a_my_project\java-web\ch06\src\main\webapp\upload.html`。
2. 表单设置 `enctype="multipart/form-data"`，提交路径为 `upload`。
3. 页面中输入学号、姓名，并选择一个或多个文件。
4. 真实后台处理类为 `D:\a_my_project\java-web\ch06\src\main\java\fun\javierchen\ch06\FileUploadServlet.java`，并使用 `@MultipartConfig` 处理上传请求。
5. Servlet 遍历 `request.getParts()`，将文件保存到 `WEB-INF/uploadFile`。
6. 返回页面显示学号、姓名、提交时间、文件数量和文件名列表。

### 对应代码
文件：`D:\a_my_project\java-web\ch06\src\main\webapp\upload.html`

```html
<form action="upload" method="post" enctype="multipart/form-data">
    <div class="row">
        <div class="field">
            <label>学号</label>
            <input type="text" name="studentId" placeholder="输入学号" required>
        </div>
        <div class="field">
            <label>姓名</label>
            <input type="text" name="studentName" placeholder="输入姓名" required>
        </div>
    </div>

    <div class="upload-area">
        <input type="file" name="files" multiple>
        <div class="upload-text">点击或拖拽上传文件</div>
        <div class="upload-hint">支持多文件同时上传</div>
    </div>

    <button type="submit" class="submit-btn">提 交</button>
</form>
```

文件：`D:\a_my_project\java-web\ch06\src\main\java\fun\javierchen\ch06\FileUploadServlet.java`

```java
@MultipartConfig(
        fileSizeThreshold = 1024 * 1024,
        maxFileSize = 1024 * 1024 * 10,
        maxRequestSize = 1024 * 1024 * 50
)
public class FileUploadServlet extends HttpServlet {

    @Override
    protected void doPost(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {
        request.setCharacterEncoding("UTF-8");
        response.setCharacterEncoding("UTF-8");
        response.setContentType("text/html;charset=UTF-8");

        String savePath = getServletContext().getRealPath("/WEB-INF/uploadFile");
        File uploadDir = new File(savePath);
        if (!uploadDir.exists()) {
            uploadDir.mkdirs();
        }

        String studentId = request.getParameter("studentId");
        String studentName = request.getParameter("studentName");
        Collection<Part> parts = request.getParts();

        for (Part part : parts) {
            String fileName = part.getSubmittedFileName();
            if (fileName != null && !fileName.isEmpty()) {
                part.write(savePath + File.separator + fileName);
            }
        }
    }
}
```

### 结果说明
上传成功后，页面会显示：
- 学号
- 姓名
- 提交时间
- 文件数量
- 文件列表
- 提交成功提示

当前仓库中已存在上传后的文件，路径如下：

- `D:\a_my_project\java-web\ch06\target\ch06-1.0-SNAPSHOT\WEB-INF\uploadFile`

说明上传功能已经成功运行并保存过文件。

### 截图说明
请在此处插入以下截图：

```md
![文件上传页面](./images/ch06-upload-page.png)
![上传成功页面](./images/ch06-upload-success.png)
![服务器保存文件结果](./images/ch06-upload-files.png)
```

---

## 核心文件清单
- `D:\a_my_project\java-web\ch01\src\main\webapp\first.jsp`
- `D:\a_my_project\java-web\ch01\src\main\java\fun\javierchen\WelcomeServlet.java`
- `D:\a_my_project\java-web\ch02\src\main\webapp\GetForm.jsp`
- `D:\a_my_project\java-web\ch02\src\main\webapp\GetTest.jsp`
- `D:\a_my_project\java-web\ch03\src\main\java\fun\javierchen\ch03\param\ParamServlet.java`
- `D:\a_my_project\java-web\ch03\src\main\java\fun\javierchen\ch03\param\RequestParamServlet.java`
- `D:\a_my_project\java-web\ch04\src\main\webapp\login.jsp`
- `D:\a_my_project\java-web\ch04\src\main\java\fun\javierchen\ch04\LoginServlet.java`
- `D:\a_my_project\java-web\ch04\src\main\webapp\welcome.jsp`
- `D:\a_my_project\java-web\ch04\src\main\webapp\failure.jsp`
- `D:\a_my_project\java-web\ch05\src\main\webapp\register.html`
- `D:\a_my_project\java-web\ch05\src\main\java\fun\javierchen\ch05\RegisterServlet.java`
- `D:\a_my_project\java-web\ch05\src\main\webapp\login1_contextpath.jsp`
- `D:\a_my_project\java-web\ch05\src\main\webapp\login2_relative.jsp`
- `D:\a_my_project\java-web\ch05\src\main\webapp\login3_server_relative.jsp`
- `D:\a_my_project\java-web\ch05\src\main\webapp\login4_page_relative.jsp`
- `D:\a_my_project\java-web\ch05\src\main\java\fun\javierchen\ch05\LoginServlet.java`
- `D:\a_my_project\java-web\ch06\src\main\webapp\upload.html`
- `D:\a_my_project\java-web\ch06\src\main\java\fun\javierchen\ch06\FileUploadServlet.java`

## 实验总结
本次实验完成了 JSP 页面编写、Servlet 创建、GET 请求传参、请求头读取、登录校验、用户注册、4 种 action 路径写法以及文件上传等核心内容。通过本次实验，我进一步理解了 Java Web 中页面与 Servlet 的协作方式、请求与响应对象的使用方法，以及数据从前端提交到后端处理再返回结果页面的完整流程。

